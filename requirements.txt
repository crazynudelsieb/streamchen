# Runtime dependencies (API + worker share one set).

# --- API ---
fastapi>=0.141.0,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.13.4,<3.0

# --- Storage ---
SQLAlchemy[asyncio]>=2.0.51,<3.0
asyncpg>=0.30,<1.0
redis>=8.0.1,<9.0

# --- Security ---
argon2-cffi>=23.1,<26.0

# --- Playback ---
# The resolver for YouTube metadata and audio. Pinned loosely on purpose:
# extraction breaks whenever YouTube changes, and the fix is always a newer
# yt-dlp. ffmpeg is a system package, installed in the worker image.
yt-dlp>=2026.7.4

# --- Templates ---
# Server-rendered pages; the frontend owns no state, so there is nothing for a
# build toolchain to do here.
jinja2>=3.1,<4.0

# --- Misc ---
python-dotenv>=1.0,<2.0
