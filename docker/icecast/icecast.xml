<icecast>
    <!--
      Audio delivery only (concept §5). No mount points are declared: the
      playback worker connects one source per room and Icecast creates the
      mount on the fly, which is what lets rooms be created and destroyed
      without touching this file.
    -->
    <location>streamchen</location>
    <admin>admin@localhost</admin>

    <limits>
        <clients>@ICECAST_MAX_CLIENTS@</clients>
        <sources>@ICECAST_MAX_SOURCES@</sources>
        <queue-size>1048576</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>20</source-timeout>
        <!-- Send a burst on connect so a listener hears audio immediately
             instead of waiting for the next buffer to fill. This is the whole
             trade-off of how quickly play() starts: the browser needs a few
             frames decoded before it will play anything, and at 128 kbps the
             stream itself only delivers 16 KB a second. It is also how far
             behind live a listener starts, so bigger is not free. -->
        <burst-on-connect>1</burst-on-connect>
        <burst-size>@ICECAST_BURST_SIZE@</burst-size>
    </limits>

    <authentication>
        <source-password>@ICECAST_SOURCE_PASSWORD@</source-password>
        <relay-password>@ICECAST_RELAY_PASSWORD@</relay-password>
        <admin-user>admin</admin-user>
        <admin-password>@ICECAST_ADMIN_PASSWORD@</admin-password>
    </authentication>

    <hostname>@ICECAST_HOSTNAME@</hostname>

    <listen-socket>
        <port>8000</port>
    </listen-socket>

    <http-headers>
        <!-- Listeners must stay live; nothing between here and the browser may
             hold on to a chunk of the stream (concept §12). -->
        <header name="Cache-Control" value="no-store,no-cache,must-revalidate" />
        <header name="Pragma" value="no-cache" />
        <header name="Access-Control-Allow-Origin" value="*" />
    </http-headers>

    <paths>
        <basedir>/usr/share/icecast2</basedir>
        <logdir>/var/log/icecast2</logdir>
        <webroot>/usr/share/icecast2/web</webroot>
        <adminroot>/usr/share/icecast2/admin</adminroot>
        <alias source="/" destination="/status.xsl" />
    </paths>

    <logging>
        <accesslog>-</accesslog>
        <errorlog>-</errorlog>
        <loglevel>2</loglevel>
        <logsize>10000</logsize>
    </logging>

    <security>
        <chroot>0</chroot>
    </security>
</icecast>
