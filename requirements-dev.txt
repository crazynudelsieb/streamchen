# Development / test dependencies (not needed at runtime).
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.3,<10.0
pytest-asyncio>=0.24,<2.0
httpx>=0.28,<1.0

# The test suite runs without Postgres or Redis: SQLite stands in for the
# database and fakeredis for the bus, so CI needs no services.
aiosqlite>=0.22.1,<1.0
fakeredis>=2.26,<3.0

# Linter. Rules live in ruff.toml; CI runs `ruff check .` as a release gate.
ruff>=0.8.0,<1.0
